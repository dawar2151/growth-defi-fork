// SPDX-License-Identifier: GPL-3.0-only
pragma solidity ^0.6.0;

contract TestGrowthDeFi
{
/*
	function _convertUSDCBalanceToDAI() internal
	{
		Assert.equal(_getBalance(Addresses.DAI), 0, "DAI balance must be 0");
		uint256 _inputAmount = _getBalance(Addresses.USDC);
		uint256 _expectedOutputAmount = _C_calcConversionOutputFromInput(Addresses.USDC, Addresses.DAI, _inputAmount);
		uint256 _expectedInputAmount = _C_calcConversionInputFromOutput(Addresses.USDC, Addresses.DAI, _expectedOutputAmount);
		Assert.isAtLeast(_expectedInputAmount, _inputAmount, "USDC input amounts must be at least");
		uint256 _outputAmount = _C_convertBalance(Addresses.USDC, Addresses.DAI, _inputAmount, 1);
		Assert.equal(_getBalance(Addresses.USDC), 0, "USDC balance must be 0");
		Assert.equal(_getBalance(Addresses.DAI), _outputAmount, "DAI balance mismatch");
		Assert.isAtLeast(_outputAmount, _expectedOutputAmount, "DAI output amounts must be at least");
	}
*/
}
